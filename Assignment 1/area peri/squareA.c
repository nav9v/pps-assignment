#include <stdio.h>
int main(){
    int area,side;
    printf("Enter the side of the square : ");
    scanf("%d",&side);
    area=side*side;
    printf("The area of the square is : %d",area);
    return 0;
}